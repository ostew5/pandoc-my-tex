# Metadata applied to both PDF and HTML builds.
# LaTeX-only variables (mainfont, fontsize, etc.) are silently ignored by HTML output.

lang: en

# Font settings — used by xelatex via fontspec.
# titlepage.tex overrides mainfont with Inter (loaded by file path) for PDF builds.
# Liberation Sans is kept here as a safe fallback if the override fails.
monofont: "Liberation Mono"

fontsize: 10pt
